# Remina Survey - Backend environment variables

# Server
PORT=3000

# Admin
# Change this in production and pass as Authorization: Bearer <token>
ADMIN_TOKEN=admin123

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=remina
